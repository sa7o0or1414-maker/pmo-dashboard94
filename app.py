import streamlit as st

# إعدادات الصفحة
st.set_page_config(
    page_title="منصة PMO",
    page_icon="📊",
    layout="wide"
)

# --- CSS لجعل الاتجاه من اليمين لليسار ---
st.markdown("""
<style>
    html, body, [class*="css"] {
        direction: rtl;
        text-align: right;
    }
    section[data-testid="stSidebar"] {
        direction: rtl;
        text-align: right;
    }
</style>
""", unsafe_allow_html=True)

# --- البار الجانبي ---
with st.sidebar:
    st.markdown("## 🧭 القائمة")
    page = st.radio(
        "",
        ["🏠 الصفحة الرئيسية", "📤 رفع البيانات", "🔐 تسجيل الدخول"]
    )

# --- محتوى الصفحات ---
if page == "🏠 الصفحة الرئيسية":
    st.title("📊 الصفحة الرئيسية")
    st.write("مرحبًا بك في منصة إدارة المشاريع (PMO).")
    st.info("هنا سيتم عرض مؤشرات الأداء ولوحات المعلومات لاحقًا.")

elif page == "📤 رفع البيانات":
    st.title("📤 رفع البيانات")
    st.write("سيتم لاحقًا رفع ملفات Excel لتحديث الداشبورد.")
    st.warning("🔒 هذه الصفحة ستُقفل لاحقًا وتتطلب تسجيل دخول.")

elif page == "🔐 تسجيل الدخول":
    st.title("🔐 تسجيل الدخول")
    username = st.text_input("اسم المستخدم")
    password = st.text_input("كلمة المرور", type="password")
    st.button("تسجيل الدخول")

